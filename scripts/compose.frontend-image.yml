# Use pre-built frontend image (built by scripts/build-frontend-with-backend.sh).
# Run: ./scripts/compose.sh -f scripts/compose.frontend-image.yml up -d
services:
  frontend:
    image: payload-frontend
    build: ~
    environment:
      PAYLOAD_API_URL: ${PAYLOAD_API_URL:-http://backend:3001}
